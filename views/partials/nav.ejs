<nav class="navbar">
  <div class="interno">
    <a href="/home" class="<%= attivo === 'home' ? 'attivo' : '' %>"><span class="ico">🏠</span>Home</a>
    <a href="/cerca" class="<%= attivo === 'cerca' ? 'attivo' : '' %>"><span class="ico">🔍</span>Cerca</a>
    <a href="/ordini" class="<%= attivo === 'ordini' ? 'attivo' : '' %>"><span class="ico">📦</span>Ordini</a>
  </div>
</nav>
