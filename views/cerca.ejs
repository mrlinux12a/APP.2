<%
  function conFiltri(cambio) {
    const stato = { q, diametro, materiale };
    Object.keys(cambio).forEach(function (k) { stato[k] = cambio[k]; });
    const parti = Object.keys(stato)
      .filter(function (k) { return stato[k]; })
      .map(function (k) { return k + '=' + encodeURIComponent(stato[k]); });
    return '/cerca' + (parti.length ? '?' + parti.join('&') : '');
  }
%>
<!DOCTYPE html>
<html lang="it">
<head>
  <%- include('partials/app_head', { titolo }) %>
</head>
<body data-loggato="1">
  <div class="app">
    <%- include('partials/appbar', { titolo, intestazione: 'Cerca prodotti', indietro: '/home' }) %>

    <div class="contenuto" style="padding-bottom:0;">
      <%- include('partials/barra_ricerca', {
            azione: '/cerca',
            q,
            ambito: { diametro: diametro || '', materiale: materiale || '' },
            segnaposto: 'Scrivi anche solo una parte, es. "valv"'
          }) %>
    </div>

    <div class="contenuto" style="padding-top:0;padding-bottom:0;">
      <div id="tag-raffinamento">
        <% if (q && tag.diametri.length) { %>
          <div class="filtro-titolo">Diametro</div>
          <div class="chip-riga">
            <a class="chip <%= diametro ? '' : 'attivo' %>" href="<%= conFiltri({ diametro: null }) %>">Tutti</a>
            <% tag.diametri.forEach(function (d) { %>
              <a class="chip <%= diametro === d.valore ? 'attivo' : '' %>"
                 href="<%= conFiltri({ diametro: d.valore }) %>"><%= d.valore %></a>
            <% }); %>
          </div>
        <% } %>
        <% if (q && tag.materiali.length) { %>
          <div class="filtro-titolo">Materiale</div>
          <div class="chip-riga">
            <a class="chip <%= materiale ? '' : 'attivo' %>" href="<%= conFiltri({ materiale: null }) %>">Tutti</a>
            <% tag.materiali.forEach(function (mat) { %>
              <a class="chip <%= materiale === mat.valore ? 'attivo' : '' %>"
                 href="<%= conFiltri({ materiale: mat.valore }) %>"><%= mat.valore %></a>
            <% }); %>
          </div>
        <% } %>
      </div>
    </div>

    <div class="contenuto" style="padding-top:0;">
      <div id="risultati">
        <% if (!q) { %>
          <div class="vuoto"><span class="emoji"><%- iconaLente %></span>Scrivi anche solo un pezzo di parola: cercando &laquo;valv&raquo; trovi tutte le valvole.</div>
        <% } else if (!risultati.length) { %>
          <div class="vuoto"><span class="emoji">🤷</span>Nessun prodotto trovato per &laquo;<%= q %>&raquo;.</div>
        <% } else { %>
          <div class="card card-fitta">
            <% risultati.forEach(function (p) { %>
              <%- include('partials/prodotto', { p, carrello }) %>
            <% }); %>
          </div>
        <% } %>
      </div>
    </div>

    <div class="barra-carrello" data-barra-carrello <%= carrelloPezzi ? '' : 'hidden' %>>
      <div class="interno">
        <div class="barra-carrello-testo">
          <span data-pendenti-wrap hidden><strong data-pendenti>0</strong> selezionati</span>
          <span data-sep-pendenti hidden> · </span>
          <span data-carrello-wrap <%= carrelloPezzi ? '' : 'hidden' %>><strong data-carrello-conta><%= carrelloPezzi %></strong> nel carrello</span>
          <span data-carrello-vuoto <%= carrelloPezzi ? 'hidden' : '' %>>Nessun pezzo nel carrello</span>
        </div>
        <div style="display:flex;gap:8px;align-items:center;">
          <button class="btn btn-primary" type="button" data-aggiungi-tutti disabled>Aggiungi</button>
          <a class="btn btn-chiaro" href="/carrello" data-vai-carrello <%= carrelloPezzi ? '' : 'hidden' %>>Vai al carrello</a>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/nav', { attivo: 'catalogo' }) %>
  <script src="/app.js" defer></script>
</body>
</html>
